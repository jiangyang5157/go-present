
// START_native_attr OMIT
android:text
android:hint
// STOP_native_attr OMIT

// START_branding OMIT
forEach <attr, resId> {

    if attr is android.R.attr.text: 
        text = repo.string(resId) // HL
    
    if attr is android.R.attr.hint:
        hint = repo.string(resId) // HL
}
// STOP_branding OMIT

